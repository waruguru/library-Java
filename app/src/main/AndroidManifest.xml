<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.moringaschool.libraryjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibraryJava">
        <activity android:name=".ui.MainActivity">
        </activity>

        <activity android:name=".ui.SignUpActivity"/>
        <activity android:name=".ui.ListActivity"/>
        <activity android:name=".ui.SliderActivity"/>
        <activity android:name=".ui.DetailsActivity">

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.moringaschool.libraryjava.ui.MainActivity"/>
        </activity>
        <activity android:name=".ui.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"/>
        </application>

</manifest>